---
type: worktree
title: "Add `apiKey?: string` to `VisionConfig` and `DEFAULT_CONFIG`."
description: "Implement Add `apiKey?: string` to `VisionConfig` and `DEFAULT_CONFIG`."
area: backend
tags: []
status: abandoned
created: "2026-08-18"
updated: "2026-08-18"
stale_after: "2026-09-01"
related: [../plans/backend-support-apikey-in-vision-proxy-config.md]
---
# Add `apiKey?: string` to `VisionConfig` and `DEFAULT_CONFIG`.

## Objective

Implement Add `apiKey?: string` to `VisionConfig` and `DEFAULT_CONFIG`.

## Scope

`src/core.ts`

## Tasks

- [ ] Implement Add `apiKey?: string` to `VisionConfig` and `DEFAULT_CONFIG`.
- [ ] Add unit tests for Add `apiKey?: string` to `VisionConfig` and `DEFAULT_CONFIG`.

## Verification

npm test

## Status

- [ ] Worktree created
- [ ] Implementation complete
- [ ] Tests pass
- [ ] Landed on feature branch (ready for PR/merge)
