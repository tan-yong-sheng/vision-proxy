---
type: worktree
title: "Mask `apiKey` in `vp config get` output."
description: "Implement Mask `apiKey` in `vp config get` output."
area: backend
tags: []
status: abandoned
created: "2026-08-18"
updated: "2026-08-18"
stale_after: "2026-09-01"
related: [../plans/backend-support-apikey-in-vision-proxy-config.md]
---
# Mask `apiKey` in `vp config get` output.

## Objective

Implement Mask `apiKey` in `vp config get` output.

## Scope

`src/commands/config.ts`

## Tasks

- [ ] Implement Mask `apiKey` in `vp config get` output.
- [ ] Add unit tests for Mask `apiKey` in `vp config get` output.

## Verification

npm test

## Status

- [ ] Worktree created
- [ ] Implementation complete
- [ ] Tests pass
- [ ] Landed on feature branch (ready for PR/merge)
