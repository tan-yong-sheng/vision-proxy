---
type: worktree
title: "Replace `imagescript` dependency with `sharp`; update `safeCropImage`, `encodeCroppedImage`, `cropImage`"
description: "Implement Replace `imagescript` dependency with `sharp`; update `safeCropImage`, `encodeCroppedImage`, `cropImage`"
area: backend
tags: []
status: active
created: "2026-08-18"
updated: "2026-08-18"
stale_after: "2026-09-01"
related: [../plans/backend-image-format-support-and-crop-decoder.md]
---
# Replace `imagescript` dependency with `sharp`; update `safeCropImage`, `encodeCroppedImage`, `cropImage`

## Objective

Implement Replace `imagescript` dependency with `sharp`; update `safeCropImage`, `encodeCroppedImage`, `cropImage`

## Scope

`src/core.ts`, `package.json`

## Tasks

- [ ] Implement Replace `imagescript` dependency with `sharp`; update `safeCropImage`, `encodeCroppedImage`, `cropImage`
- [ ] Add unit tests for Replace `imagescript` dependency with `sharp`; update `safeCropImage`, `encodeCroppedImage`, `cropImage`

## Verification

npm test

## Status

- [ ] Worktree created
- [ ] Implementation complete
- [ ] Tests pass
- [ ] Landed on feature branch (ready for PR/merge)
