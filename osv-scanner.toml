# OSV-Scanner ignore policy for vision-proxy.
# Accepted risks for dependencies that are not exposed to untrusted input
# or external network connections in this CLI tool.

[[IgnoredVulns]]
id = "CVE-2025-71329"
reason = "image-size is used for local file path resolution only; never parses untrusted image data from external sources"

[[IgnoredVulns]]
id = "GHSA-w3rx-r6r6-pgpr"
reason = "image-size is used for local file path resolution only; never parses untrusted image data from external sources"
